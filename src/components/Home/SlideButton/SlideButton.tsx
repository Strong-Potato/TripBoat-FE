import styles from "./SlideButton.module.scss";

interface PropsType {
  right: boolean;
  itemWidth: number;
}

function SlideButton({ right, itemWidth }: PropsType) {
  return <div></div>;
}

export default SlideButton;
