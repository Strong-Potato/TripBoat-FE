import CreatVoteTitleModal from "@/components/vote/CreatVoteTitleModal";

const VoteDetail = () => {
  return (
    <div>
      VoteDetail
      <CreatVoteTitleModal />
    </div>
  );
};

export default VoteDetail;
